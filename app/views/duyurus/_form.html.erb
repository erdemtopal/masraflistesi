<%= form_with(model: duyuru) do |form| %>
  <% if duyuru.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(duyuru.errors.count, "error") %> prohibited this duyuru from being saved:</h2>

      <ul>
        <% duyuru.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin_id %>
    <%= form.text_field :admin_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
